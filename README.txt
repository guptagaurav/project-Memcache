

1. To see the webpage goto artickly.com/simpleop.

2. Since ipage doesn't support Memcache. Currently supported BY google App Engine, AWS, Heroku.

3. Therefore right now trying to deploy the app on with memcache enabled on Heroku. And simultaneously trying to 
	migrate from MySQL to Postgres on Heroku.

4. Included a repositry to that includes files without memcache use under 'without-memcache.git'.


